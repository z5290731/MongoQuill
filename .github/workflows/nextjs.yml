
# This file is now redundant and handled by deploy.yml
# Keeping it minimal to prevent conflicts.
name: Skip
on: [workflow_dispatch]
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Handled by deploy.yml"
